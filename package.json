{
  "name": "zikos",
  "version": "0.1.0",
  "description": "AI Music Teacher POC",
  "private": true,
  "scripts": {
    "lint": "eslint 'static/**/*.js' || true",
    "lint:fix": "eslint --fix 'static/**/*.js' || true",
    "format": "prettier --write 'static/**/*.{js,html,css}'",
    "format:check": "prettier --check 'static/**/*.{js,html,css}'"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "prettier": "^3.2.5"
  }
}

